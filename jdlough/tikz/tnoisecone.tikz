  \begin{loglogaxis}[
    xlabel={Frequency $\left( Hz \right)$},
    ylabel={Thermal Noise $\left( m/\sqrt{Hz} \right)$},
    grid=minor,
  ]
  \addplot[blue] table {python/tnoisecone.dat};

  \end{loglogaxis}

